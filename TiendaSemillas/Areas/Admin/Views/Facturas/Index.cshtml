@model IEnumerable<TiendaSemillas.Areas.Admin.Models.Factura>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Total)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaDeCompra)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Total)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaDeCompra)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FacturaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FacturaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FacturaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
